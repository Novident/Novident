/// used by workspace manager to apply the view type
enum WorkspaceViewType {
  /// Shows all the nodes into the current container
  ///
  /// Usually is used when a folder is selected to show the current
  /// nodes into it
  tree,

  /// Shows the Novident editor
  editor,
}
